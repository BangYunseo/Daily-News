# 로컬 실행에 필요한 환경변수 목록(견본).
# 실제 값은 이 파일이 아니라 .env 에 적는다. (.env 는 커밋되지 않음)
# 사용법:  이 파일을 복사해 .env 로 만들고, 각 값을 채운 뒤  `python main.py`

# --- 필수 ---
GEMINI_API_KEY=
RESEND_API_KEY=
MAIL_TO=

# --- 선택 (비워두면 코드 기본값 사용) ---
# GEMINI_MODEL=gemini-3.1-flash-lite
# MAIL_FROM=뉴스 브리핑 <onboarding@resend.dev>
